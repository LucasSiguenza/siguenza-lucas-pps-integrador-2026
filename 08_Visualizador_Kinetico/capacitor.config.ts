import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'marlujo.visualizador.kinetico',
  appName: '08_Visualizador_Kinetico',
  webDir: 'www'
};

export default config;
